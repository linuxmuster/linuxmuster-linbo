linuxmuster-linbo 32/64 build instructions
==========================================

Install 64bit Ubuntu 16.04.2/3.16.0 desktop.

Try to build:

./buildpackage.sh

Install all mentioned build dependencies (a lot of them).

Now build:

./buildpackage.sh
