linuxmuster-linbo 32/64 build instructions
==========================================

Install 64bit Ubuntu 13.10 desktop.

Add 32bit Architecture:
dpkg --add-architecture i386
apt-get update

Install build files:
apt-get install dpkg-dev

Try to build:

./buildpackage.sh

Install all mentioned build dependencies (a lot of them).

Now build:

./buildpackage.sh

